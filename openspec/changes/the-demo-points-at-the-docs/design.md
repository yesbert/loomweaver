## Context

See `proposal.md` — Why. What stands before the discussion:

- The docs site publishes `docs/` at loomweaver.dev, one page per guide, with anchors.
- The demo is a product built on the published packages, at demo.loomweaver.dev, with its own
  README explaining what is there and why.
- The workbench offers a few places a demo could hang a pointer on without new chrome: a bar item, a
  tooltip, a command in the palette, a view action, a menu entry. Which of them fits is part of the
  discussion.

## Goals / Non-Goals

**Goals:**

- A visitor of the demo can find out, for a feature they are looking at, what it is and where it is
  explained.
- The result of the discussion is written here so the build has one source.

**Non-Goals:**

- No decision in this note yet. Every section below is filled by the discussion, not before it.

## Decisions

To be written by task 1.1.

## Risks / Trade-offs

- **The pointers make the demo look like documentation.** → Part of the discussion: the pointer
  must not cost the demo its credibility as a product.
