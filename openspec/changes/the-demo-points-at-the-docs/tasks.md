## 1. The discussion

- [ ] 1.1 Discuss, with `/opsx:explore`, what a visitor should learn from the demo without leaving
      it, which features are worth pointing at, what the pointer looks like, where it lives, and
      whether it is the demo's own or a platform feature. Write the result into `design.md`.
- [ ] 1.2 Update this change from the result: the remaining tasks below, and a spec delta with
      `skip_specs` removed where the platform's guarantees change.

## 2. The build

- [ ] 2.1 Shaped by task 1.2.
