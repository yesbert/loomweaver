## 1. A settings row may be led by a symbol

- [x] 1.1 Add the optional symbol to the row and draw it before the label, hidden from anything
      reading the row out.
- [x] 1.2 Test that a row with a symbol draws it, that a row without one is unchanged, and that the
      label is what names the row either way.

## 2. The identity block is lendable

- [x] 2.1 Let a caller pin the narrow form, and follow the viewport where it asks for nothing.
- [x] 2.2 Publish the block, with the documentation the published surface requires, and name it where
      the machine-readable contract lists what a distribution may embed.
- [x] 2.3 Test both forms: pinned narrow, and following the frame.

## 3. Close it out

- [x] 3.1 Say in the guide for products that the row takes a symbol and that the identity block can
      be placed elsewhere.
- [x] 3.2 Run the shell's tests, the packaging gate and the repository's guards.
