## 1. Establish what is there before changing it

- [x] 1.1 Record what `@loom/frame-kit` ships today: the manifest fields it declares, the files in
      the tarball, and the fact that it carries no `main`, no `types` and no `exports`. This is the
      before-picture the byte comparison and the manifest guard are measured against.

      Recorded: `@loom/frame-kit` 0.5.49 declares `name`, `version`, `description`, `keywords`,
      `license`, `author`, `publishConfig` and `files: ["dist"]`. No `main`, no `types`, no
      `exports` — confirmed rather than assumed. The tarball carries three files:
      `lw-elements.global.js`, `penpal.global.js` and `lw-frame.css`. Their digests are the
      reference task 6.1 compares against.
- [x] 1.2 Read `docs/authoring-a-weaver.md` and answer the open question in design.md: does it name
      the five shapes, or only demonstrate the calls? Write the answer here — it decides whether the
      documentation demand in task 4.2 is a sentence or a section.

      Answered: **only the calls.** `authoring-a-weaver.md` teaches `LwFrame.state.watch`,
      `LwFrame.applySurfaceState`, `LwFrame.state.apply` and `LwFrame.connectState` with worked
      examples, and names not one of the five shapes. So task 4.2 is a section rather than a
      sentence, and the guide gains what the examples have been implying all along.

## 2. Emit the description

- [x] 2.1 Mark the bootstrap export internal so the compiler strips it, and add that marker to the
      comment guard's directive list. Without both halves the marker is itself a comment on an
      unpublished symbol.

      Both halves done, and the second is not bureaucracy: with the marker in place and the guard
      unchanged, the bootstrap's own line would have been reported as a comment on something no
      longer published. `@internal` instructs the compiler exactly as `@ts-…` does.
- [x] 2.2 Extend `build.mjs` to emit declarations for the bundled entry alongside the bundle, and to
      append the ambient block that declares the global the script installs. One command produces
      both or neither.

      Emitted through the TypeScript API from the same entry esbuild bundles, then turned from a
      module into a **global script** by stripping the top-level `export` keywords and appending
      `declare const LwFrame: LwFrameApi;`. That shape is the point: a module would type an import
      nobody writes, while a global script gives an author the global and all five shapes from one
      reference.

      One explanatory comment had to come out of `build.mjs` again. The guard reads that file, the
      reasoning already sits in design.md, and `emitGlobalScriptDeclaration` says in its name what
      the paragraph was saying.
- [x] 2.3 Stamp the emitted declaration with the same version banner the bundle carries, from the one
      version source. Confirm the comment guard treats it as generated and does not scan it.

      Stamped: `@loom/frame-kit 0.5.49 — lw-frame.d.ts. Generated by build.mjs; do not edit.` The
      guard skips it twice over — `dist` is a skipped directory, and the banner matches the
      generated-file rule.
- [x] 2.4 Confirm the emitted declaration is self-contained: the five shapes reference only
      primitives and each other, so it must pull in nothing from the shell or the SDK. If it does,
      the emit is reaching further than the surface does and needs narrowing.

      Confirmed, and enforced rather than observed: the build step throws if any `import` or `export`
      survives the transformation. The emitted file is 43 lines and names nothing beyond primitives,
      `Record` and the five shapes.

## 3. Publish it

- [x] 3.1 Add the `types` entry to the manifest and make sure `files` carries the declaration.
- [x] 3.2 Verify with `check-package-exports` that the promise is real: it must fail if `types` names
      a file the tarball does not ship. Break it on purpose once to see it fail, then put it back.

      It bites: with the declaration moved aside the guard reports `@loom/frame-kit ·
      dist/lw-frame.d.ts` as resolving to nothing. Its own header carried a note that frame-kit
      "declares no entry points at all, so the promise check cannot see it" — no longer true, so the
      note was corrected in the same breath.
- [x] 3.3 Add frame-kit to the packed declarations the guards read, beside the three packages that
      already ship them.

      Two guards, and the second needed work the design note had not foreseen. `check-api-docs`
      collected published names through the module symbol, and a global script has none — it threw
      rather than reporting. It now falls back to the top-level declarations of a script file, which
      is the honest reading: what a global script declares at the top level is exactly what a
      consumer can name.

## 4. Close what that opens

- [x] 4.1 `check-comments` now sees the five shapes as contract documentation. Empty
      `comment-residue.json` and confirm the checker fails on the stale entry if it is left behind.

      Empty again. Left behind, the entry fails with "down to 0 from 6 — trim the residue to 0". The
      guard now reads 325 reachable declarations rather than 319: the six frame-kit names.
- [x] 4.2 `check-api-docs` now demands prose for every name frame-kit publishes. Close the gap in the
      guides rather than by exemption, unless a name genuinely carries no prose — and then list it
      with its reason, the way the checker's existing exemptions are.

      Closed in the guide, no exemptions added. `LwFrame` was already named there; the five shapes
      were not, exactly as task 1.2 found. `authoring-a-weaver.md` gains a passage under *The frame
      UI kit* that shows the `tsconfig` line and says what each shape is for. 268 published exports,
      0 undocumented.

## 5. Prove it works for the consumer it is for

- [x] 5.1 Type-check a surface against the shipped declaration the way an author would: reference it
      from a `tsconfig`, call the global, and confirm a wrong method name and a wrong argument are
      both reported. A declaration that ships but does not bite is the failure this change is about.

      Done as an author would: a throwaway project whose `tsconfig` names `@loom/frame-kit` in
      `types`, nothing imported. A correct surface compiles clean. A wrong one produces all three
      failures the change exists to catch:

      - `Property 'wathc' does not exist on type 'LwStateApi'. Did you mean 'watch'?`
      - `Type '"purple"' is not assignable to type '"light" | "dark" | undefined'.`
      - `Expected 2 arguments, but got 1.`
- [x] 5.2 Confirm a surface that ignores the declaration still runs unchanged — plain HTML with a
      script tag is the majority case and must not have acquired a build step.

      The three served artifacts are byte-identical to `main`, so nothing a plain surface loads has
      changed. The declaration is a fourth file beside them that such a surface never fetches.

## 6. Verify

- [x] 6.1 The bundle output is byte-identical to `main`. This change adds a description beside the
      artifacts; if the artifacts themselves moved, something reached further than intended.

      `lw-elements.global.js`, `lw-frame.css` and `penpal.global.js` compare byte-for-byte against
      the digests recorded in task 1.1. The `@internal` marker on the bootstrap does not survive
      minification, which is why the bundle did not move.
- [x] 6.2 The packed declarations of the other five packages are byte-identical to `main`.

      Zero lines of difference across all 53 declaration files.
- [x] 6.3 All seven guards pass, the residue is empty, and the test count is unchanged.

      Seven green, residue empty, lint clean, 1288 tests across 148 files.
- [x] 6.4 `npx openspec validate --all --strict` passes.
