## 1. Leave out what cannot be permitted

- [x] 1.1 Add a failing test: a plugin the distribution declared not optional, holding only the right
      to contribute, is drawn as a section today.
- [x] 1.2 Drop such a row where the list of rows is built, keeping a plugin that has a switch and one
      whose capabilities are listed without toggles.
- [x] 1.3 Test that a surface left with nothing shows its own empty line.

## 2. Close it out

- [x] 2.1 Check the guide for the permissions surface still describes what the page shows.
- [x] 2.2 Run the shell's tests and the repository's guards.
