/** Visual weight of a button. */
export type LwButtonVariant =
  | 'primary'
  | 'default'
  | 'success'
  | 'danger'
  | 'warning'
  | 'info'
  | 'ghost';

/** Button size; `md` is the default, `sm` for dense chrome (toasts, footers). */
export type LwButtonSize = 'sm' | 'md';
