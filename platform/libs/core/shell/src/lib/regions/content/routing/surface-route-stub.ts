import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'lw-surface-route',
  imports: [RouterOutlet],
  template: '<router-outlet />',
})
export class SurfaceRouteStub {}
