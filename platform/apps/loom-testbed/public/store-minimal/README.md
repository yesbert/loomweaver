# Store plugin (minimal)

The smallest possible community plugin: one page at `/store-minimal`, no settings, and only the
`contributions` permission.

Use it for the contrast with **Store plugin (full)**: a plugin without declared settings gets no
entry under the _Community plugins_ settings group.
