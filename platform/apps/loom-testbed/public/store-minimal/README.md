# Store plugin (minimal)

The smallest possible community plugin: one static dashboard tab, no settings, only the
`contributions` permission.

Use it to see the contrast with **Store plugin (full)** — a plugin without declared settings gets no entry
under the *Community plugins* settings group.
